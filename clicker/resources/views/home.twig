{% extends 'templates/app.twig'%}

{% block content %}
	<div class="row">
		
		{% for index, quiz in quizes %}
			<div class= "col-md-4 col-sm-6">
				{% include 'quizes/partials/item.twig' with {quiz: quiz} %}
			</div>
			{% if (index+1)%3 ==0 %}
				<div class="row">
					
				</div>
			{%endif%}

		{%endfor%}
	</div>
	
{% endblock %}